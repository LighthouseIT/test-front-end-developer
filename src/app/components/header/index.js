import React from 'react'

import Icon from '../icon'

const Header = props => {
    let prefix = 'header'

    return (
        <header className={prefix}>
        </header>
    )
}

export default Header