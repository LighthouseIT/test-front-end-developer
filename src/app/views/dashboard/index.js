import React from 'react'

import View from '../../components/view'

const Places = () => {
    const prefix = 'places'

    return (
        <div className={ prefix }>
        </div>
    )
}

export default Places